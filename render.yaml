services:
  - type: web
    name: fileforge
    runtime: node
    plan: free
    region: oregon
    buildCommand: npm run build
    startCommand: npm run start:prod
    envVars:
      - key: NODE_ENV
        value: production
