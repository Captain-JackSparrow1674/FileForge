services:
  - type: web
    name: fileforge
    runtime: node
    plan: free
    region: oregon
    buildCommand: npm run build
    startCommand: node server/server.js
    envVars:
      - key: NODE_ENV
        value: production
